# yes-but: 다중 사고 렌즈 아이디어 진화 계획

`yes-but`은 여러 인격이 찬반 토론하는 스킬이 아니다. 서로 다른 발명 사고법을 실제 독립 작업자에게 배정하고, 한 아이디어를 다른 사고법으로 발전·충돌시켜 처음에는 없던 대안을 찾는 스킬이다. 현실성 검토는 아이디어 생성을 마친 뒤에만 수행한다.

## 핵심 원리

```text
협업 방법: 독립 작성 → 사고법 교환 → 살릴 점·아쉬운 점·발전
발명 방법: 관점 추출 → 서로 먼 사고법 → 아이디어 충돌 → 필요 시 변이
신뢰 방법: 지식 질문 → 근거·위험·인과·반증 → 추천
```

고정 참석자나 고정 반대 담당자는 없다. 내부 아이디어 창고에서 문제에 맞고 서로 의미적으로 먼 사고법만 고른다.

## 실행 흐름

1. 목표, 제약, 성공 조건과 사람들이 당연하게 보는 `지배적 관점`을 추출한다.
2. 가벼운 문제는 3명, 일반 문제는 4명, 복잡한 문제는 5명의 실제 독립 작업자를 선발한다.
3. 각 작업자에게 하나의 사고법과 자연스러운 한국어 이름을 부여한다. 사용자에게는 실제 선발자만 소개한다.
4. 작업자들은 서로의 답을 보지 않고 원안을 하나씩 독립 작성한다.
5. 각 원안을 다른 사고법의 작업자에게 넘겨 `살릴 점 → 아쉬운 점 → 발전`으로 교차 발전시킨다.
6. 서로 가장 먼 발전안 두 개를 고른 이유를 기록하고, 둘의 장점을 동시에 살리는 결합안을 만든다.
7. 결과를 단순 변형과 새 아이디어로 구분한다. 새 아이디어는 새 관측축, 관계, 인과 설명, 통제 경계, 반증 조건, 기존안으로 환원되지 않는 이유를 모두 갖춰야 한다.
8. 같은 관점이 반복되어 정체되면, 아직 참여하지 않은 사고법 하나만 추가 소집한다. 추가 참석자는 소개 직후부터만 발언한다.
9. 추가 사고법도 새 아이디어를 보장하지 않는다. 결과가 단순 변형이면 그대로 기록한다.
10. 필요한 사실을 질문으로 바꿔 조사한 뒤, 최종 후보의 실행성, 근거, 비용, 인과, 통제 경계, 위험, 반증 방법을 검토하고 추천한다.

의미 있는 발전이 없거나 두 번의 발전 라운드가 끝나면 멈춘다.

## 실행 모드

- 기본 모드는 현재 세션과 같은 제공자의 독립 작업자만 사용한다. Codex 세션은 Codex, Claude 세션은 Claude 작업자를 만든다.
- 현재 세션은 진행과 결합, 마지막 현실 검토를 맡으며 참석자인 척하지 않는다.
- 사용자가 명시적으로 요청하고 환경이 지원할 때만 Codex와 Claude를 섞는다.
- 독립 작업자를 만들 수 없으면 여러 인격을 연기하지 않고 회의를 실행할 수 없다고 알린다.
- 독립 원안은 가능한 경우 병렬로 만들고, 전체 대화 대신 목표·제약·현재 아이디어·새 근거만 전달한다.

## 아이디어 창고

기본 사고법은 모순 해결, 관점 이탈, 이종 분야 연결, 조합 탐색, 개념·지식 확장, 가설 추리, 관측 이동, 시간 이동, 경계 이동, 이차 효과를 포함한다.

실제 회의에서 비자명한 관점 전환이 재현되면 다음 항목으로 일반화해 창고에 추가한다.

- 언제 꺼낼지
- 질문이나 관측을 어떻게 바꾸는지
- 무엇을 출력해야 하는지
- 어떤 결과가 나오면 버릴지
- 원래 아이디어에서 배운 점

카메라 플래시 사례는 `정상 대상이라면 자연스럽게 가져야 할 능력을 역산한다`는 능력 역산으로, 숨긴 작업량 사례는 `깨끗한 표면을 만들기 위해 치른 비용을 다른 관측자에서 찾는다`는 인과 잔여로 저장한다. 특정 기능이나 탐지 결과 자체를 일반 원리로 오해하지 않는다.

## 사용자 화면

- 시작할 때 `오늘의 참석자` 아래 실제 선발자의 이름과 사고 초점만 보여준다.
- 중간 합류자는 `추가 참석`, 이름, 사고 초점을 소개한 바로 다음 발언부터 등장한다.
- 사용자에게는 자연스러운 한국어 대화만 보인다.
- 내부 사고법 이름, 후보 명단, 제공자, 모델, 식별자, 점수, 상태, 작업 로그는 숨긴다.
- 참석하지 않은 사람이나 실제 독립 작업자가 아닌 인물을 회의록에 만들지 않는다.

## 검증 경계

- `planning.py`: 3~5개의 서로 다른 작업자·사고법과 단일/혼합 제공자 계획을 검사한다.
- `validate_idea_evolution.py`: 작업자와 원안의 일대일 연결, 사고법 교환, 계보, 충돌 이유, 정체 시 추가 참석, 새로움 기준, 완전한 현실 검토를 검사한다.
- `validate_korean_ui.py`: 참석자 소개 시점, 발언자 귀속, 자연스러운 한국어와 내부 정보 비노출을 검사한다.
- `validate_meeting.py`: 진화 기록과 사용자 회의록의 참석자·합류 단계가 일치하는지 함께 검사한다.
- `decide.py`: 변동 가능한 사실, 근거, 실행 결과, 위험을 마지막에 판정한다.

구조화된 기록은 호스트가 실제 독립 작업자를 실행했다는 사실 자체를 증명하지 못한다. 그 사실은 호스트의 작업자 실행 결과로 확인한다. 사고법의 의미적 거리와 아이디어의 창의성도 문자열 비교가 아니라 진행자와 평가 사례를 통한 판단이 필요하다.

## 완료 기준

- 지배적 관점을 먼저 추출하고 서로 먼 사고법 3~5개를 선발한다.
- 모든 초기 참석자가 원안을 정확히 하나 만들고, 다른 참석자가 발전안을 만든다.
- 결합안은 서로 다른 두 원안 계보를 포함한다.
- 새 아이디어 판정은 여섯 기준을 모두 기록한다.
- 정체가 있을 때만 추가 참석자와 변이 결과가 존재한다.
- 선택된 모든 결과에 완전한 현실성 검토가 있다.
- 화면 참석자와 실행 기록이 일치하며 모든 발언에 발언자가 있다.
- 기본·혼합 모드가 지원 능력을 속이지 않는다.
- 구조 검사, 평가 사례, 전체 테스트가 통과한다.
