# yes-but: 아이디어 진화 회의 스킬 계획

`yes-but`은 여러 에이전트가 낸 서로 다른 아이디어를 평가부터 하지 않고 먼저 발전시킨다. 각 아이디어의 강점을 살리고 약점을 보완한 뒤, 결합안과 처음에는 없던 제3안을 만든다. 현실성 검토는 아이디어 생성이 끝난 다음에만 수행한다.

## 핵심 가치

> 여러 에이전트가 서로의 아이디어를 이어받아, 누구도 처음에는 제안하지 않았던 더 나은 대안을 만든다.

고정된 반대 검토자는 두지 않는다. 다른 아이디어를 받으면 반드시 다음 세 가지로 발전시킨다.

- `YES`: 반드시 살릴 구체적인 강점
- `BUT`: 거절이 아니라 해결할 개선 기회
- `BUILD`: 강점을 유지한 구체적인 발전안

## 진행 흐름

1. **발산:** 탐험가들이 서로 다른 원안을 독립적으로 만든다.
2. **교차 발전:** 모든 원안에 `keep`, `but`, `build`를 작성하고 원안별 발전안을 만든다.
3. **결합:** 서로 다른 두 개 이상의 원안 계보를 합친 결합안을 만든다.
4. **변이:** 기존 안의 선택이나 반복이 아닌 새로운 제3안을 만든다.
5. **현실 검토:** 기술, 근거, 비용, 위험을 확인한다.
6. **추천:** 발전안, 결합안, 제3안을 비교하고 불확실성과 다음 행동을 함께 제시한다.

의미 있는 발전이 더 이상 없거나 두 번의 발전 라운드가 끝나면 멈춘다. 화면에는 실제로 달라진 아이디어만 짧게 보여준다.

## 역할

- **진행자:** 목표, 제약, 성공 조건을 정리하고 최종 추천을 설명한다.
- **탐험가:** 기본 세 명이 독립 원안을 만들고 서로의 원안을 발전시킨다.
- **합성자:** 원안별 발전안, 결합안, 제3안을 만든다.
- **현실 검토자:** 합성이 끝난 뒤에만 실행 가능성, 근거, 비용, 위험을 확인한다.

기본은 현재 세션과 같은 제공자의 독립 탐험가 세 명을 생성하는 단일 제공자 모드다. Codex 세션이면 Codex 탐험가만, Claude 세션이면 Claude 탐험가만 생성한다. 현재 세션은 진행과 합성을 맡고, 탐험가들은 서로의 결과를 보기 전에 원안을 만든다. 사용자가 명시적으로 요청하고 실행 환경이 지원할 때만 Codex와 Claude 탐험가를 섞는다.

## 내부 구조와 사용자 화면

내부에서는 아이디어의 원안, 부모 계보, 발전 검토, 결과 종류를 구조화한다. 검증기는 다음 경계를 확인한다.

- 원안이 둘 이상이며 각 원안에 발전 검토와 발전안이 있는가
- 모든 검토에 `keep`, `but`, `build`가 있는가
- 결합안이 서로 다른 두 원안의 계보를 실제로 포함하는가
- 제3안이 별도 아이디어이며 기존 문장을 그대로 반복하지 않는가
- 발전안, 결합안, 제3안이 모두 있어야 현실 검토로 넘어가는가

사용자에게는 `진행자`, `탐험가`, `합성자`, `현실 검토자`의 자연스러운 한국어 발언만 보인다. 영문 역할명, 제공자명, 모델명, 내부 번호, 상태, 필드, 점수, 작업 로그는 노출하지 않는다.

## 기존 판정 기능의 위치

기존 주장·근거 판정기는 제거하지 않는다. 아이디어 생성을 방해하는 앞단 심사가 아니라, 발전된 결과를 실행하기 직전 확인하는 마지막 품질 점검으로 사용한다.

```text
발산 → 교차 발전 → 결합 → 변이 → 주장·근거 판정 → 추천
```

## 속도와 성능

- 단순 요청에는 회의를 열지 않는다.
- 독립 원안은 가능한 경우 동시에 만든다.
- 각 역할에는 전체 대화 대신 목표, 제약, 현재 아이디어, 새 근거만 전달한다.
- 같은 의미의 아이디어와 조사 요청은 합친다.
- 발전은 최대 두 라운드로 제한하고 의미 있는 변화가 없으면 일찍 끝낸다.
- 현실 검토는 최종 후보에만 수행하며 기존 증분 판정과 근거 재사용을 유지한다.
- 화면에는 모든 내부 작업이 아니라 결론에 영향을 준 변화만 묶어서 보여준다.

## 파일 구성

```text
yes-but/
├── SKILL.md
├── references/
│   ├── meeting-rules.md
│   ├── korean-ui.md
│   └── decision-rules.md
├── schemas/
│   ├── idea-evolution.schema.json
│   ├── meeting-state.schema.json
│   └── meeting-event.schema.json
├── scripts/
│   ├── validate_idea_evolution.py
│   ├── validate_korean_ui.py
│   ├── planning.py
│   └── decide.py
└── examples/
```

## 완료 기준

- 기본 회의가 세 개의 서로 다른 원안을 만든다.
- 모든 원안이 `YES → BUT → BUILD`를 거쳐 발전한다.
- 결합안은 서로 다른 원안 계보를 둘 이상 포함한다.
- 제3안은 기존 안의 선택이나 문장 반복이 아니다.
- 현실성 검토는 발전안, 결합안, 제3안이 나온 뒤에만 시작한다.
- 사용자 화면은 자연스러운 한국어만 사용하며 내부 정보를 노출하지 않는다.
- 기본 모드는 현재 제공자의 독립 탐험가 세 명을 생성하며, 지원되지 않으면 역할극으로 대체하지 않는다.
- 믹스 모드는 지원 능력을 확인하고, 지원되지 않으면 한계를 분명히 알린다.
- 검증 도구와 전체 테스트가 통과한다.
