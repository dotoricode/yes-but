# yes-but: 한국어 에이전트 회의 스킬 계획

`yes-but`은 여러 에이전트가 제안, 반론, 사실 확인을 나눠 수행하고 하나의 결론을 만드는 스킬이다. 내부 처리는 구조화하되, 사용자에게는 실제 회의처럼 짧고 자연스러운 한국어 대화만 보여준다.

## 핵심 원칙

- 단순한 요청에는 회의를 열지 않는다.
- 필요한 역할만 참여시키고 독립적인 작업은 동시에 수행한다.
- 의견보다 근거를 우선하고, 결론에 영향을 주는 쟁점만 다시 토론한다.
- 내부 식별자, 점수, 상태, 작업 로그는 사용자에게 노출하지 않는다.
- 사용자에게 보이는 모든 발언과 상태 문구는 자연스러운 한국어로 작성한다.
- 의미 없는 맞장구, 반복 발언, 가짜 입력 연출은 만들지 않는다.
- `SKILL.md`에는 실행에 꼭 필요한 규칙만 두고 상세 내용은 참조 파일로 분리한다.

## 사용자가 보는 회의

회의에는 필요에 따라 다음 역할이 참여한다.

- **진행자:** 요청을 정리하고 쟁점을 좁힌다.
- **제안자:** 서로 다른 해결책을 제시한다.
- **반대 검토자:** 실패 가능성과 숨은 전제를 찾는다.
- **사실 확인자:** 결론을 바꿀 수 있는 사실만 확인한다.
- **판단 담당자:** 근거와 위험을 비교해 결론을 정리한다.

화면에는 판단에 영향을 준 발언만 시간순으로 보여준다.

> 진행자: 먼저 선택해야 할 쟁점을 두 가지로 좁혀 보겠습니다.
>
> 제안자: 첫 번째 방법이 가장 빠르지만, 사용 환경이 달라지면 추가 대응이 필요합니다.
>
> 반대 검토자: 그 방법은 연결이 끊겼을 때 복구할 수단이 부족합니다.
>
> 사실 확인자: 공식 자료를 확인한 결과, 해당 환경에서는 자동 복구를 지원합니다.
>
> 판단 담당자: 복구 위험이 해소됐으므로 첫 번째 방법을 권합니다.

영어 역할명, 영어 상태명, `C01` 같은 내부 번호, 데이터 필드, 모델 이름은 표시하지 않는다. 코드나 제품명처럼 사용자가 제공한 원문을 인용해야 하는 경우에도 설명 문장은 한국어로 유지한다.

## 내부 처리와 화면 표시의 분리

내부에서는 주장, 근거, 반론, 전제, 중요도, 판정 상태를 구조화해 관리한다. 이 자료가 판단의 기준이며 화면 대화는 그 변화를 설명하는 표현 계층이다.

화면 문구는 별도 모델을 다시 호출하지 않고, 역할의 한국어 결론과 정해진 한국어 문장 규칙으로 만든다. 표시 직전 검사에서 다음 항목이 발견되면 사용자에게 보내지 않고 한국어로 다시 작성한다.

- 내부 식별자와 영어 상태
- 시스템 이벤트 이름과 데이터 필드
- 사용자에게 의미 없는 점수와 작업 로그
- 설명되지 않은 영문 문장

이 구조는 내부 정확성과 화면의 자연스러움을 함께 유지하며, 번역용 호출과 출력 지연도 줄인다.

## 회의 진행

1. 진행자가 목표, 선택할 문제, 제약, 성공 조건을 정리한다.
2. 요청을 바로 답할지, 사실만 확인할지, 일부 역할만 부를지 판단한다.
3. 필요한 역할이 서로의 결론을 보지 않은 상태에서 동시에 검토한다.
4. 결과를 주장 단위로 합치고 중복을 제거한다.
5. 규칙 기반 판정으로 채택, 기각, 확인 필요, 이견 상태를 구분한다.
6. 결론을 바꿀 가능성이 큰 쟁점만 최대 두 번 다시 검토한다.
7. 진행자가 권고안, 선택 이유, 제외한 대안, 남은 불확실성, 다음 행동을 정리한다.

중요한 주장이 채택 또는 기각됐거나 추가 검토가 결론을 바꾸기 어렵다면 회의를 끝낸다. 의미 있는 이견이 남으면 합의한 것처럼 표현하지 않는다.

## 원본 계획안에서 발전시킬 부분

원본은 병렬 역할, 주장 장부, 규칙 기반 판정, 선택적 재토론이라는 좋은 기반을 갖고 있다. 다음 부분은 보완이 필요하다.

- 모든 종류의 주장에 같은 점수식을 적용하면 사실, 제안, 위험의 차이를 놓칠 수 있다.
- 현재 판정 코드는 전체 주장을 매번 다시 계산하며, 이미 판정된 작업을 조기에 중단하는 규칙이 없다.
- 영향이 큰 주장에 대한 약한 근거 검사 일부는 앞선 통과 조건과 중복돼 실제로 추가 보호를 제공하지 않는다.
- 근거 재사용과 최신성 기준이 없어 같은 조사를 반복할 수 있다.
- 내부 이벤트가 영어와 식별자를 포함하므로 그대로 사용하면 한국어 전용 화면 원칙을 위반한다.
- 긴 회의의 전체 대화를 다시 전달하면 토큰 사용량과 응답 시간이 계속 증가한다.

## 속도와 성능 개선

- **적응형 역할 선택:** 요청의 위험과 불확실성에 따라 최소 역할만 호출한다.
- **동시 실행:** 제안, 반대 검토, 사실 확인처럼 독립적인 작업은 함께 시작한다.
- **짧은 문맥:** 전체 대화 대신 목표, 제약, 현재 쟁점, 새 근거만 각 역할에 전달한다.
- **증분 판정:** 새로 생기거나 바뀐 주장만 다시 계산한다.
- **중복 제거:** 같은 의미의 주장과 조사 요청을 실행 전에 합친다.
- **근거 재사용:** 출처와 확인 시점을 기록하고 최신성 조건을 만족할 때 재사용한다.
- **조기 종료:** 결론을 바꿀 수 없는 작업은 취소하고 남은 예산을 쓰지 않는다.
- **작업 예산:** 제안 수, 미해결 쟁점 수, 재토론 횟수, 도구 호출 수에 상한을 둔다.
- **묶음 갱신:** 화면 이벤트를 짧은 간격으로 모아 한 번에 갱신한다.
- **측정:** 첫 유효 발언 시간, 전체 소요 시간, 호출 수, 재사용률, 재토론률을 기록해 개선 효과를 비교한다.

판정은 진실을 대신하는 점수가 아니라 다음 작업을 고르는 장치로 사용한다. 사실은 근거의 직접성과 최신성, 제안은 실행 가능성과 효과, 위험은 발생 가능성과 피해를 중심으로 각각 판정한다.

## 파일 구성

스킬은 작게 유지한다.

```text
yes-but/
├── SKILL.md                    # 실행 조건과 핵심 회의 절차
├── references/
│   ├── meeting-rules.md        # 역할, 재토론, 종료 규칙
│   ├── korean-ui.md            # 한국어 표시와 금지 표현
│   └── decision-rules.md       # 종류별 판정 기준
├── schemas/
│   ├── meeting-state.schema.json
│   └── meeting-event.schema.json
├── scripts/
│   ├── decide.py               # 증분 판정
│   └── validate_korean_ui.py   # 화면 출력 검사
└── examples/
    └── meeting.md              # 짧은 한국어 회의 예시
```

`SKILL.md`는 언제 회의를 열고, 어떤 역할을 부르며, 언제 끝내는지만 설명한다. 구현 세부 사항과 긴 체크리스트를 넣지 않는다.

## 구현 순서

1. 짧은 `SKILL.md`와 역할 선택 규칙을 만든다.
2. 내부 회의 상태와 종류별 판정 규칙을 만든다.
3. 한국어 회의 문구 생성과 노출 검사를 만든다.
4. 병렬 실행, 중복 제거, 조기 종료를 연결한다.
5. 예시 회의로 정확성, 자연스러움, 호출량을 검증한다.
6. 실제 실행 시간을 측정한 뒤 증분 판정과 근거 재사용을 최적화한다.

## 완료 기준

- 단순 요청은 불필요한 회의 없이 처리한다.
- 필요한 역할만 실행되고 독립 작업은 동시에 진행된다.
- 사용자 화면에 영어 상태, 내부 식별자, 시스템 로그가 나타나지 않는다.
- 모든 사용자용 발언이 짧고 이해하기 쉬운 한국어다.
- 근거, 반론, 불확실성, 결론이 대화에서 구분된다.
- 같은 주장과 조사를 불필요하게 반복하지 않는다.
- 재토론과 호출 예산을 넘지 않고 회의가 종료된다.
- 최종 답변에 권고안, 이유, 제외한 대안, 남은 불확실성, 다음 행동이 포함된다.
- 검증 도구와 예시 회의가 한국어 화면 규칙을 통과한다.
